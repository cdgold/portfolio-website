/*import React from "react"
import styled from "styled-components"
import { faReact, faHtml5, faNodeJs, faCss3Alt, faPython, faJava, faSquareJs } from "@fortawesome/free-brands-svg-icons"
import { faLeaf, faDatabase, faChessPawn, faGamepad } from "@fortawesome/free-solid-svg-icons"

const REACT_ICON_COLOR = "#4d91ff"
const HTML_ICON_COLOR = "#ffc012"
const DATABASE_ICON_COLOR = "#336791"
const LEAF_ICON_COLOR = "#589636"
const NODEJS_ICON_COLOR = "#215732"
const CSS_ICON_COLOR = "#264de4"
const PYTHON_ICON_COLOR = "#ffd343"
const JAVA_ICON_COLOR = "#fc0303"
const EXPRESS_ICON_COLOR = "#808080"
const CPLUSPLUS_ICON_COLOR = "#00349c"
const JAVASCRIPT_ICON_COLOR = "#007acc"

enum IconType {
  React = "react",
  Html = "html",
  Sql = "sql",
  Mongodb = "mongodb",
  Node = "node",
  Css = "css",
  Python = "python",
  Java = "java",
  Express = "express",
  CPlusPlus = "cplusplus",
  Typescript = "typescript",
  Javascript = "javascript"
}

export function isIconType(str: string): str is IconType {
  return Object.values(IconType).map(v => v.toString()).includes(str);
} 

const CPlusPlusIcon = styled.span`
  color: ${CPLUSPLUS_ICON_COLOR};
  font-family: sans-serif;
  font-weight: 900;
  font-size: 18px;
`

interface TechIconProps {
  icon: IconType;
}

const TechIcon = (props: TechIconProps) => {
  if (icon ==)
  return(
    <div>

    </div>
  )
}

export default TechIcon
*/